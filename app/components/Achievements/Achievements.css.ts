import { style } from "@vanilla-extract/css"

export const achievementsScreen = style({
  textAlign: "center",
})

export const achievementList = style({
  display: "flex",
  flexWrap: "wrap",
  gap: "1rem",
  alignContent: "center",
  alignItems: "start",
  justifyContent: "center",
})
